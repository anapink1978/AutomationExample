{
  "scripts": {
    "test": "playwright test",
    "test:ui": "sh -c 'if [ -n \"$DISPLAY\" ]; then playwright test --ui; else xvfb-run -a playwright test --ui; fi'",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "dotenv": "^17.3.1",
    "typescript": "^6.0.2"
  }
}
